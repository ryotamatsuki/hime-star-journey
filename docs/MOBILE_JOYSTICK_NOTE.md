# Mobile joystick

- Smartphone/coarse-pointer exploration replaces the four-button D-pad with a drag joystick.
- The joystick is fixed on the lower-left side of the screen and supports diagonal movement.
- Releasing/cancelling the pointer clears all virtual direction inputs immediately.
- The notebook button moves to the lower-right on touch layouts to avoid overlap.
- Keyboard controls are unchanged.
